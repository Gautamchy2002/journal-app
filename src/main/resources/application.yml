
spring:
#  redis:
#

#    for local
#    host: localhost
#    port: 6379
  data:
    redis:
      url: redis://default:DrIftwsuxDLOLbIXwZSPXdbIRWgThqSe@produce-prairie-communicative-71228.db.redis.io:15575

    mongodb:
      uri: ${MONGO_URI}
      database: journaldbs
      auto-index-creation: true

  mail:
    host: smtp.gmail.com
    port: 587
    username: ${MAIL_USERNAME}
    password: ${MAIL_PASSWORD}
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true

server:
  port: ${PORT:8081}
  servlet:
    context-path: /journal

weather:
  api:
    key : ${WEATHER_API_KEY}


